services:
  backend:
    build: ./backend
    restart: always
    # env_file:
    #   - ./backend/.env
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    restart: always
    # env_file:
    #   - ./frontend/.env
    ports:
      - "3001:3001"
    depends_on:
      - backend